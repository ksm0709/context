// registry.ts removed — functionality migrated to opencode-global-settings.ts
// Tests for MCP registration now live in src/shared/opencode-settings.test.ts
import { describe, it } from 'vitest';

describe('omx/registry (removed)', () => {
  it('placeholder — file deleted, no tests needed', () => {
    // registry.ts was removed as part of the OMX plugin system removal
  });
});
