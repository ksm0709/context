[2026-03-26 22:05:00] Investigated why legacy `context_mcp` appears after `context update omx`. Findings: `update` has no `omx` subcommand, so `omx` is treated as a path target (`/home/bear1130/repositories/context/omx`). Current install code writes `context-mcp`, but live `~/.claude/settings.json` still contains `context_mcp`, implying the external `claude mcp add` flow likely normalizes hyphens to underscores on write. `~/.omx/mcp-registry.json` correctly uses `context-mcp`. Also found stale injected guidance still references `context_mcp_*` tool names in `src/shared/knowledge-context.ts` and `src/lib/scaffold.ts`.
[2026-03-26 22:21:00] Created implementation plan `.omx/plans/fix-update-omx-mcp-normalization.md` for the `context update omx` / `context_mcp` issue. Plan scope: add explicit `update omx` routing, isolate OMX-only updates from OMC reinstall side effects, broaden OMC detection to both `context-mcp` and `context_mcp`, add Claude settings normalization helper, and align CLI help/docs/tests.
[2026-03-26 22:44:00] Implemented the `context update omx` fix: added explicit `update omx` routing, isolated OMX-only updates from OMC reinstall, broadened OMC detection to both `context-mcp` and `context_mcp`, and added Claude settings normalization after OMC install. Verification so far: targeted tests 57/57 passed, full suite 202/202 passed, TypeScript diagnostics 0 errors, raw build succeeded, and isolated tmux QA passed with a fake Claude CLI proving `install omc` normalizes a legacy `context_mcp` write to `context-mcp` while `update omx` does not invoke Claude again. Remaining in-progress work: architect sign-off and Ralph-required deslop/re-regression finalization.
[2026-03-26 22:54:00] Implemented the `context update omx` / legacy `context_mcp` fix. Added explicit `update omx` routing, broadened OMC detection to both `context-mcp` and `context_mcp`, added Claude settings normalization after `install omc`, updated CLI help/docs, and added regression tests. Verification: targeted tests 57/57, full suite 202/202, tsc diagnostics 0 errors, raw build succeeded, isolated tmux QA passed with a fake Claude CLI that wrote legacy `context_mcp` and confirmed normalization plus no Claude calls during `update omx`.
[2026-03-26 23:08:00] Bumped package version to 0.0.33 for npm publish readiness, aligned package.json/package-lock.json/bun.lock, added a 0.0.33 CHANGELOG entry, verified Vitest 202/202 + tsc diagnostics clean + npm pack dry-run, then committed and pushed `a19f243 Prepare 0.0.33 for the pending npm publish` to `origin/main`.
[2026-03-26 23:32:00] Fixed Bun local-update failure after the 0.0.33 release. Root cause: published package.json accidentally declared `@ksm0709/context` as a dependency of itself, which Bun surfaced as an integrity/extraction failure during `context update plugin` (`bun add @ksm0709/context@latest`). Added `src/package-metadata.test.ts` to assert no self-dependency, removed the self-dependency, regenerated bun.lock, bumped to 0.0.34, inspected the packed tarball package.json to confirm the dependency is gone, and pushed commit `d7b2941 Unblock Bun-based plugin updates after the 0.0.33 release`.
[2026-03-26 23:45:00] Investigated OMX `mock-mcp` startup warning. Root cause is not `~/.omx/mcp-registry.json` (that file only contains `context-mcp`). Current `~/.codex/config.toml` still has a standalone `[mcp_servers.mock-mcp]` block at lines 68-71 pointing to `/home/bear1130/repositories/context/mock-mcp.js`, but that file does not exist. Reproduced with `bun /home/bear1130/repositories/context/mock-mcp.js` -> `Module not found`, which explains Codex/OMX showing `handshaking with MCP server failed: connection closed: initialize response`. `mock-mcp` also sits outside the `Managed by omx setup` registry-sync block, so `omx setup` is preserving it rather than creating/removing it.
[2026-03-26 23:46:00] No-op turn finalization only. Loaded the project plugin-dev skill, read today’s note, and ran `mise run lint && mise run test` for required verification. Lint emitted existing warnings only; tests failed in `src/package-metadata.test.ts` because current working-tree `package.json` again declares the package as a dependency of itself (`@ksm0709/context: ^0.0.34`). No code changes were made in this turn.
[2026-03-26 23:47:00] Second no-op turn-end injection handled. No new work performed. Reused prior verification context from the immediately preceding turn: lint warnings only and test failure remains in `src/package-metadata.test.ts` due to `package.json` self-dependency on `@ksm0709/context` at `^0.0.34`. No files changed in this turn beyond this note.
[2026-03-26 23:58:00] 사용자 요청으로 `/home/bear1130/repositories/context` 저장소에서 main 기준 새 git worktree를 생성했다. 현재 원본 저장소가 이미 `main`을 체크아웃 중이고 작업트리가 dirty여서 충돌을 피하려고 sibling 경로 `/home/bear1130/repositories/context-main`에 `git worktree add ../context-main --detach main`을 사용했다. 검증 결과 worktree 목록에 새 경로가 추가되었고 새 worktree HEAD는 `d7b2941`(detached)이다.
[2026-03-26 23:54:07] [2026-03-27 00:01:00] 사용자 요청으로 새 worktree `/home/bear1130/repositories/context-main`에서 작업 브랜치 `chore/context-main-20260326`를 생성했다. 기존 worktree는 detached HEAD였고, 로컬/원격 충돌을 피하려고 날짜 기반 브랜치명으로 `git -C ../context-main switch -c chore/context-main-20260326`를 실행했다. 검증 결과 새 worktree의 현재 브랜치는 `chore/context-main-20260326`이다.
