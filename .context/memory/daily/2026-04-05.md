[2026-04-05 23:01:43] - Fixed GitHub workflow publishing issue by adding `NODE_AUTH_TOKEN` environment variable linked to `secrets.NPM_TOKEN` and creating a `.npmrc` file dynamically in both `publish.yml` and `release.yml`.
- Resolved circular dependency issue in `package.json` that was failing the `package-metadata.test.ts`.
[2026-04-05 23:03:36] - Fixed a `TypeError` (`undefined is not an object (evaluating 'lastUserMsg.info.time.created')`) in `experimental.chat.messages.transform` hook (`src/index.ts`) by adding optional chaining and a nullish coalescing default value.
[2026-04-05 23:11:31] - Attempted to push a new version (1.4.0) to npm via GitHub workflow `release.yml`.
- The GitHub workflow failed at `Publish to npm` step due to `npm error code EOTP`, indicating that the `NPM_TOKEN` requires a One-Time Password (2FA is enforced).
- To fix this, you will need to replace the `NPM_TOKEN` secret with an NPM Granular Access Token (automation token) that can bypass 2FA for CI/CD, or change the package's 2FA settings to not require OTP for publish operations.
[2026-04-05 23:14:49] - Note: User mentioned that NPM token registration was already correctly configured in the previously deleted `release-please` workflow. We can reference its history if we need to see how it was done.
[2026-04-05 23:30:24] - Investigated the failure of the new OIDC publish approach. Realized that the original, deleted `release-please` workflow used a specific setup involving `repository_dispatch` to trigger a separate publish workflow rather than doing it inline within the `release.yml`.
- Restored the old `release-please` publish execution structure by reverting the npm token authentication attempt, re-introducing the `repository_dispatch` trigger in `release.yml` (using `peter-evans/repository-dispatch`), and updating `publish.yml` to support the `repository_dispatch` trigger. This aligns with the previous, functioning OIDC setup.
